
<nav>
  <% if user_signed_in? && current_user.admin? %>
    <p class="nav hero"><%= link_to 'TestGuru Admin Panel', admin_tests_path %></p>
  <% else %>
    <p class="nav hero"><%= link_to 'TestGuru', root_path %></p>
  <% end %>
   

  <% if user_signed_in? %>
    <p class="nav user"><b><%= t('.username', name: current_user.name) %></b> |
     <%= link_to t('.logout'), destroy_user_session_path, method: :delete %></p> 
  <% else %>
    <p class="nav user"> <%= link_to t('.login'), new_user_session_path %> | 
    <%= link_to t('.signup'), new_user_registration_path %> </p>
  <% end %>
</nav>