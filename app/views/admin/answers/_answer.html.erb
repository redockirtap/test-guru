<li>
  <p><%= answer.body %></p>
  <%= link_to t('links.show'), admin_answer_path(answer) %> |
  <%= link_to t('links.edit'), edit_admin_answer_path(answer) %> |
  <%= link_to t('links.delete'), admin_answer_path(answer), method: :delete, data: {confirm: "Are you sure?" } %>
  </li>