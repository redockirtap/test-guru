<% if resource.present? && resource.errors.any? %>

  <%= content_tag :div, class: 'alert alert-danger resource-errors', role: 'alert', data: { resource_id: resource.id } do %>
    <%= render "devise/shared/error_messages", resource: resource %>
  <% end %>
<% end %>